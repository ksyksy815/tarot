const Footer = () => {
  return (
    <footer className={"flex justify-center w-full bg-gray-200 h-[30vh]"}>
      <div
        className={`h-[80px] w-full flex items-center justify-between max-w-screen-xl`}
      >
        footer
      </div>
    </footer>
  );
};

export default Footer;
